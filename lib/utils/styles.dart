import 'package:flutter/material.dart';

import 'colors.dart';

const secondaryStyle = TextStyle(
    fontSize: 15, color: secondaryText, fontWeight: FontWeight.normal);
const secondaryStyleDark =
    TextStyle(fontSize: 17, color: accentColor, fontWeight: FontWeight.normal);
const numberStyle = TextStyle(fontSize: 25, color: accentColor);
const titleCardStyle = TextStyle(fontSize: 25, color: accentColor);
const buttonStyle =
    TextStyle(color: Colors.white, fontSize: 16, fontWeight: FontWeight.w600);
const whiteText = TextStyle(color: Colors.white);
